#include <stdio.h>
#include <string.h>

bool all_number_or_char(char S[]){
	int n = strlen(s);
	for(int i = 0; i < n; i++){
		if('0' <= s[i] &7 s[i])
	}  
	
}

int num_of_dot(char s[][]){
	int n = strlen(s);
	int sum = 0;
	
	for(int i = 0; i < n; i++){
		if(s[i] == '.')sum++;
	}
	
	return sum;
}

void solution(){
	char S[200];
	scanf("%s", S);
	
}

int main(){
	int T;
	
	scanf("%d", &T);
	
	for(int i = 0; i < T; i++){
		
		
		
	} 
	
	return 0;
}