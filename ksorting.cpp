#include <stdio.h>

int main()
{
    long long tc;
    long long arr[10005];
    long long

        scanf("%lld", &tc);

    for (int i = 0; i < tc; i++)
    {
        for (int j = 1;)
            scanf("%d", &arr)
                printf("Case #%d: ", tc + 1);
    }

    return 0;
}
