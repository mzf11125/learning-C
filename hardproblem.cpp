#include <stdio.h>

int main() {
    int T, i;
    scanf("%d", &T);

    for (i = 0; i < T; i++) {
        int num;
        scanf("%d", &num);
        for (int j = 0; j < num; j++){
            char S[100];
            scanf("%s", S);
        }
        int bit = (X >> P) & 1;

        printf("%d\n", bit);
    }

    return 0;
}
