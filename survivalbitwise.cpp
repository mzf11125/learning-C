#include <stdio.h>

int main(){
    int T, N, K;

    for(int i = 0; i < T; i++){
        scanf("%d", &N);
        scanf("%d", &K);
        for (int i = 0; i < N; i++) {
            scanf("%d", &roomNumbers[i]);
        }
    }


    return 0;
}