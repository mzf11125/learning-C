#include <stdio.h>



int main(){
	int T, N;
	
	scanf("%d", &T);
	scanf("%d", &N);

	int result[T][N];	
	for (int i = 0; i < T; i++){
		for (int i = 0; i < N; i++){
			scanf("%d ", &result[i]);
			result[i] =+;
		}	
	}
	printf("%d", result);
}