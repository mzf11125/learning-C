#include <stdio.h>

int main() {
    long long X, Y, Z;
    scanf("%lld %lld %lld", &X, &Y, &Z);

    // Use while loops instead of for loops for your conditions.
    while (X < 100) {
       
    }

    while (Y > 0) {
        
    }

    if (X == Z && Y == Z) {
        printf("1\n");
    } else {
        printf("-1\n");
    }

    return 0;
}