#include <stdio.h>

int main() {
    int T, N;

    scanf("%d", &T);
    scanf("%d", &N);

    for(int i = 0; i < T; i++){
        for(int j = 0; j < N; j++){
            scanf("%d", &j);

            
        }

    }
    return 0;
}
